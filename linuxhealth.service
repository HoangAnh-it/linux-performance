[Unit]
Description=Description-python-service
After=multi-user.target

[Service]
Type=simple
Restart=always
User=root
ExecStart=/bin/bash -c 'cd  path/to/this/project && ./dist/main'

[Install]
WantedBy=multi-user.target